server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Clean URLs: try exact URI, URI + .html, or folder
    location / {
        try_files $uri $uri.html $uri/ =404;
    }

    # Optional: explicitly serve 404 page
    error_page 404 /404.html;
}
